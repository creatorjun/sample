# docker-compose.yml
services:
  sample-api:
    build: .
    container_name: sample-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
